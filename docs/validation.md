# 验证记录 — 2026-09-21

范围：搭载 NVIDIA 独显的 Razer 笔记本调试。其他机型和 GPU 配置尚未验证。

环境：Ubuntu 22.04.5 LTS，Razer Blade 16 RZ09-0510，USB 1532:02b7，普通内核 6.8.0-136-generic，NVIDIA 580.126.09。

| 项目 | 结果与边界 |
|---|---|
| 单元测试 | 18 项通过：供电状态、拔电恢复、协议封包、权限、租约、温度回退、静音上限、GRUB 解析 / 参数限制 / 失败恢复、GPU XML |
| 双风扇只读探测 | 实机成功，初次读数 2400 / 2300 RPM |
| 后台风扇服务 | 已安装并运行；普通用户可读取双风扇状态 |
| 手动风扇控制 | 经认证发送 4000 RPM 目标，观察实际转速从约 2700/2500 上升到 3400/3500；按用户安静偏好提前停止测试，成功恢复自动。未宣称已到达 4000 RPM 稳态 |
| 噪声偏好 | 默认仅监测；用户主动应用后才手动控制。默认候选目标 / 上限 3000 RPM，不自动启用高转速 |
| 失联 / 拔电 / 高温 | 单元测试验证回退逻辑；尚未通过拔电和高负载实体试验验证全部组合 |
| NVIDIA | 已读取 RTX 4080 Laptop、显存和 Xorg / 远程桌面进程；nvitop 1.6.2 `--once` 实机正常 |
| 显示器 | eDP-1 3840×2400 120Hz、外接 DP-1-3 3840×2160 60Hz；配置入口交给 GNOME |
| GTK 界面 | 七页在隔离 Xvfb 桌面渲染；README 使用实际界面截图 |
| Debian 包 | 构建成功，实际安装，服务与特权助手就绪 |
| 内核可用性 | 文件 / 驱动检查通过：6.8.1-1046-realtime、5.15.0-1114-realtime、6.8.0-136/138-generic；其他条目按运行时状态显示 |
| 内核重启 | 未执行真实内核切换或重启；解析、拒绝缺失模块、失败恢复用单元测试验证 |
| 扬声器 | 开机服务成功、动态匹配 hwC1D0；本轮未重新做听音或合盖唤醒测试 |

内核与驱动可能在系统更新后改变，GUI 每次重新检查，不把这份记录当作固定的可切换清单。

## 后续人工验收

- 拔电后手动风扇会话解除，插电不会自行恢复手动风扇。
- 管理器正常关闭和异常退出后，确认风扇回到自动。
- 合盖、唤醒、外接屏重连，核对黑屏与声道。
- 选择目标内核、保存工作后重启，检查 `uname -r`、`nvidia-smi`、Wi-Fi、声音和外接屏；再次重启确认原默认内核恢复。
